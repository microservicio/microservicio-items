spring:
  application:
    name: servicio-items
server:
  port: 8082

client:
  rest:
    application:
      name: servicio-productos
    url: http://localhost:8081/
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
